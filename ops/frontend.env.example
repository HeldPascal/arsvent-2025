# Frontend environment example (production defaults)
# Copy to your env dir (e.g. env/frontend.env) and fill in values.

# Optional: API base URL for the frontend. If empty, frontend uses same-origin.
VITE_BACKEND_URL=
