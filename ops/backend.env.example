# Backend environment example (production defaults)
# Copy to your env dir (e.g. env/backend.env) and fill in values.

# Allowed: development | staging | production
APP_ENV=production
# Allowed: true | false
IS_PRODUCTION=true

# Allowed: production | development
# production enables secure cookies and trust proxy behavior
NODE_ENV=production
PORT=4000

# SQLite: file:/app/data/prod.db | file:/app/data/staging.db
DATABASE_URL=file:/app/data/prod.db
# Example: redis://redis:6379 or redis://127.0.0.1:6379
# If unset/unreachable, backend falls back to in-memory sessions (not for prod)
REDIS_URL=redis://redis:6379

# Application id and secret from the Discord app
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
# Must match Discord app redirect; use https in staging/production
DISCORD_CALLBACK_URL=https://example.com/auth/discord/callback

# Long random secret used for session cookies
SESSION_SECRET=
# Must match frontend origin (scheme + host + port)
FRONTEND_ORIGIN=https://example.com

# Optional: always-elevated admin
# SUPER_ADMIN_DISCORD_ID=

# Optional: session lifetime in ms (default: 1209600000)
# Longer sessions reduce re-login but increase exposure if leaked
# SESSION_MAX_AGE_MS=

# Optional: override content token secret (defaults to SESSION_SECRET)
# CONTENT_TOKEN_SECRET=
